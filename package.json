{
  "repository": "https://github.com/dcposch/scramble-ui",
  "description": "Scramble.io UI components made with React and Browserify",
  "dependencies": {
    "react": "*",
    "bootstrap": "*"
  },
  "devDependencies": {
    "reactify": "*",
    "browserify": "*",
    "watchify": "*",
    "http-server": "*"
  },
  "scripts": {
    "start": "npm run copy && watchify -t [reactify --harmony] jsx/*.jsx -o build/app.js & http-server build/",
    "copy": "mkdir -p build && cp index.html build/ && cp node_modules/bootstrap/dist/css/bootstrap.min.css build/"
  }
}
