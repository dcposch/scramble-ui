{
  "name":"scramble-ui",
  "version":"0.0.1",
  "main":"src/index.jsx",
  "repository": "https://github.com/dcposch/scramble-ui",
  "description": "Scramble.io UI components made with React and Browserify",
  "keywords": [
    "react",
    "react-component",
    "scramble"
  ],
  "dependencies": {
    "react": "*",
    "react-bootstrap": "*"
  },
  "devDependencies": {
    "browserify": "*",
    "reactify": "*",
    "watchify": "*",
    "http-server": "*",
    "bootstrap": "*"
  },
  "browserify": {
    "transform": [
      [
        "reactify",
        {
          "es6": true
        }
      ]
    ]
  },
  "scripts": {
    "start": "npm run copy && (watchify doc/*.jsx -o build/app.js & http-server -c-1 build/)",
    "copy": "mkdir -p build && cp doc/index.html build/ && cp -r node_modules/bootstrap/dist/* build/"
  }
}
